# Python Basic Logging Example

Run from this directory:

```bash
PYTHONPATH=../../packages/python/src python main.py
```

The example writes JSON logs to `../../logs/python-basic.log`. Start the local stack with `make observability-up`, then search the logs in Grafana Explore.
