# Python Basic Logging Example

Run from this directory:

```bash
PYTHONPATH=../../packages/python/src python main.py
```

The example writes JSON logs to `../../logs/python-basic.log`. Start the Grafana stack with `make grafana-stack-up`, then search the logs in Grafana Explore.
