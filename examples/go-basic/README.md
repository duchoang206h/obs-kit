# Go Basic Logging Example

Run from this directory:

```bash
go run .
```

The example writes JSON logs to `../../logs/go-basic.log`. Start the local stack with `make observability-up`, then search the logs in Grafana Explore.
